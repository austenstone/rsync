name: RSync

on:
  push:
  pull_request:
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - run: echo ${{ SERVER_SSH_KEY }} > key.pem
      - run: rsync README.md ${{ REMOTE_USER }}@${{ REMOTE_HOST }}:~/ -e "ssh -i .ssh/key.pem" -avzr
